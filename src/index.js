'use strict';

var ReactivePromise = require('./ReactivePromise');
var createFactory = require('./ReactivePromise-createFactory');

module.exports = ReactivePromise;
module.exports.createFactory = createFactory;
